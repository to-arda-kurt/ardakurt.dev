import styles from '../../styles/components/Footer.module.css';

export const Footer = () => {
  return (
    <footer className={styles.Footer}>
      <div className={styles.FooterWrapper}>Footer progress...</div>
    </footer>
  );
};
